interface:
  display_name: "Security Preflight"
  short_description: "Run read-only security preflights with evidence."
  default_prompt: "Use $security-preflight to run a read-only security preflight; inspect without modifying files or systems, preserve secrets, report findings-first with evidence, and distinguish repository evidence from unverified external state."
