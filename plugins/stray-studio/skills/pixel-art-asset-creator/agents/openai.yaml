interface:
  display_name: "Pixel Art Asset Creator"
  short_description: "Create, repair, and package pixel-art style sprites, tilesets, icons, and small asset sheets."
  default_prompt: "Plan, create, repair, or package this pixel-art asset. Define the asset contract and select prompt-only, generation, repair, or package-only mode first. Use image generation only when visual output is requested, and load the bundled script workflow only when deterministic slicing, QA, previews, conversion, or packaging is actually needed."
