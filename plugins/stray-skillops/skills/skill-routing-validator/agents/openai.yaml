interface:
  display_name: "Skill Routing Validator"
  short_description: "Validate positive and near-miss Codex skill routing cases."
  default_prompt: "Use $skill-routing-validator to validate this local skill set's trigger boundaries with versioned positive and near-miss cases, run the structural validator, and report behavioral checks separately from structural checks."
