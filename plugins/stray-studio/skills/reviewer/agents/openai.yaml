interface:
  display_name: "Reviewer"
  short_description: "Review and repair code, docs, UI, skills, plugins, prompts, evals, releases, and compliance preflights."
  default_prompt: "Review this artifact findings-first. Use focused subagent review passes when available, cite evidence, fix confirmed issues when requested, validate the fixes, and repeat until no actionable findings remain or a clear blocker is reached."
