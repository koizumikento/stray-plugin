interface:
  display_name: "Reviewer"
  short_description: "Run findings-first reviews and explicitly requested bounded review-fix loops."
  default_prompt: "Review this artifact findings-first. Default to review-only; edit only if I explicitly ask for a review-fix loop. Cite evidence, separate confirmed issues from assumptions, and in fix mode map each bounded change and validation result back to a finding."
