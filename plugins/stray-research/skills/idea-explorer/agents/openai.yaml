interface:
  display_name: "Idea Explorer"
  short_description: "Explore research-backed ideas from current web signals."
  default_prompt: "Use $idea-explorer to widen the option space from current web signals. Generate distinct directions, merge duplicates, cite material signals, and offer only a provisional ranking plus the next narrowing step; hand final selection, scope, acceptance criteria, and validation planning to product design."
