interface:
  display_name: "Reviewer"
  short_description: "Run findings-first reviews and bounded review-fix loops."
  default_prompt: "Use $reviewer to review this artifact findings-first; default to review-only, edit only for an explicit bounded review-fix loop, cite evidence, and map fixes and validation results back to findings."
